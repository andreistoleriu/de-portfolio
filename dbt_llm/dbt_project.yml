name: dbt_llm
version: '1.0.0'
config-version: 2

profile: dbt_llm

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]

target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  dbt_llm:
    staging:
      +schema: staging
      +materialized: view
    marts:
      +schema: marts
      +materialized: table
