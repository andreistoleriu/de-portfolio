version: 2

models:
  - name: stg_customers
    description: "Cleaned customer base"
    columns:
      - name: customer_id
        tests:
          - unique
          - not_null
      - name: segment
        tests:
          - accepted_values:
              arguments:
                values: ["vip", "regular", "occasional"]
